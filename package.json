{
  "name": "fuelflow-3-monorepo",
  "version": "1.0.0",
  "private": true,
  "packageManager": "npm@10.0.0",
  "workspaces": [
    "apps/web",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "db:generate": "turbo run db:generate"
  },
  "devDependencies": {
    "turbo": "latest"
  },
  "dependencies": {
    "jsonwebtoken": "^9.0.3"
  }
}
